# api-automation-beginner

זהו פרויקט האוטומציה הראשון שלי ל-API. השתמשתי ב-Postman כדי לבדוק קריאות GET מול JSONPlaceholder. הבדיקות כוללות אימות סטטוס קוד 200 ובדיקת תקינות הנתונים.
עדכון: הוספתי בדיקת POST שיוצרת משאב חדש בשרת ומאמתת שהתקבל סטטוס 201 (Created).
